{
  "routes": [
    {
      "method": "GET",
      "path": "/categories",
      "handler": "category.findCategory",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/categories/:id",
      "handler": "category.findOneCategory",
      "config": {
        "policies": []
      }
    }
  ]
}
