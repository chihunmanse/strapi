{
  "routes": [
    {
      "method": "GET",
      "path": "/products",
      "handler": "product.findProduct",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/products/:id",
      "handler": "product.findOneProduct",
      "config": {
        "policies": []
      }
    }
  ]
}
